# Toto-2.0 (Datadog) — isolated because toto-ts pins an older numeric stack:
# torch 2.11->2.7, numpy 2.2.6->1.26.4, pandas 2.3.3->2.2.3, and jupyter==1.1.1.
# Nothing here is wrong; it simply cannot coexist with timesfm 2.0.2 in one env.
# Confirmed on build: this env resolves torch 2.7.0 / numpy 1.26.4 while core gets
# torch 2.13.0 / numpy 2.2.6.
toto-ts==0.2.0

# toto-ts -> lightning 2.3.3 calls pkg_resources.declare_namespace at import time.
# setuptools 81 REMOVED pkg_resources, and Python 3.12 no longer bundles it, so a fresh
# env fails at `import lightning` with ModuleNotFoundError. Pin below the removal.
setuptools<81
